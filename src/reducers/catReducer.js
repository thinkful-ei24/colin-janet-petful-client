

const initialState = {
  currentCat:{},
  loading: false,
  error: null
}

const catReducer = (state=initialState, action) =>{
// if(action.type===FETCH_CAT_SUCCESS){
//   return Object.assign({},state, {currentCat:action.cat, loading:false} )
// }
// else if(){

// }
// else if(){
  
// }

return state;
};

export default catReducer;